import React from 'react';
import styles from './footer.module.css'

const Footer = (props) => (
	<footer className={ styles.footer }>
		<strong className={styles.title}> Copyright HamJu </strong>
	</footer>
);

export default Footer;